package com.interactiveresume.Interactive.Resume.Backend.data.mapping;

import com.interactiveresume.Interactive.Resume.Backend.data.dtos.CVExperienceDTO;
import com.interactiveresume.Interactive.Resume.Backend.data.models.CVExperience;

public class CVExperienceDTOMapper implements DTOMapper<CVExperienceDTO, CVExperience> {
    @Override
    public CVExperienceDTO mapModel(CVExperience cvExperience) {
        return null;
    }

    @Override
    public CVExperience mapDTO(CVExperienceDTO cvExperienceDTO) {
        return null;
    }
}
